import Counter from "@/components/Counter";

const ReactPage = () => {
  
  return (
    <div>
      <h1>This is the react page!</h1>
      <Counter />
    </div>
  )
};

export default ReactPage;


// By default EVERYTHING is SERVERSIDE
// You don't have access to react hooks = O