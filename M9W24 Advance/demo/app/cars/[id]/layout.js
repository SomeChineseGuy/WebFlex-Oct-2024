export const metadata = {
  title: "Cars",
  description: "This is all of the cars page!",
};

export default function CarsAllLayout({ children }) {
  return (
    <html lang="en">
      <body
        
      >
        {children}
      </body>
    </html>
  );
}