// includes - loop through array and see if this value is inside of it 
const arr = [1,2,3,4,5]
const newArr = [{username: 'steve'}, {username: 'sarah'}]
// console.log(newArr.includes('sarah'));
// console.log(newArr.some(item => item.username === 'sarah'))
// some 

// every
const names = ["Jason", "Jeff", "Sarah"];
const everyNameisJason = names.every(name => name.length > 2)
console.log(everyNameisJason);
// Map
// Filter
// Reduce
// Find 
// includes

